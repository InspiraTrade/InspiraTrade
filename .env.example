# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/inspiratrade

# Session Secret (generate with: openssl rand -base64 32)
SESSION_SECRET=your-random-secret-key-here-min-32-chars

# Admin Password (change to something strong)
ADMIN_PASSWORD=inspira2024admin

# Environment
NODE_ENV=development

# Server Port (API)
PORT=8080

# Frontend
BASE_PATH=/
